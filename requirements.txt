# Essential dependencies (required for backend to run)
fastapi==0.104.1
uvicorn[standard]==0.24.0
requests==2.31.0
python-dotenv==1.0.0

# Risk Engine (Required for risk analysis)
numpy>=1.24.0  # Monte Carlo simulations

# ML Libraries for Sports Brains
pandas>=2.0.0
nfl_data_py>=0.3.0  # Latest version is 0.3.3
hockey_scraper>=1.4.0

# AI Reasoning Engine (Optional - Heavy dependencies)
# To enable AI reasoning, install:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121  # GPU
# pip install transformers accelerate bitsandbytes
# Or run: python setup_ml.py (automated setup)

# Optional: Heavy ML libraries (install separately if needed for NBA brain)
# Install in separate venv for NBA brain:
# xgboost>=2.0.0  # Gradient Boosting for NBA/NFL models
# scikit-learn>=1.3.0
# tensorflow>=2.13.0  # Alternative to PyTorch
